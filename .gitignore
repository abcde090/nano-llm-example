# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
terraform.tfvars
!terraform.tfvars.example
# Note: .terraform.lock.hcl is intentionally NOT ignored — commit the lock
# files (terraform/ and terraform/bootstrap/) so CI and teammates resolve
# identical provider versions.

# Rendered manifests (Cloud Build staging)
rendered/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Editors / OS
.DS_Store
.idea/
.vscode/
